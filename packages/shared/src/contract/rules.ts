/**
 * Transport contract types for rules.
 *
 * Mirrors `RuleRecord` in `packages/core/src/types/asset.ts`. Kept separate so
 * the contract layer has a dedicated file for rule-related DTOs.
 */

export type {RuleRecord} from "./assets";