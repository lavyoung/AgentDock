/**
 * Transport contract types for scenarios.
 *
 * Mirrors `ScenarioRecord` in `packages/core/src/types/asset.ts`. Kept separate
 * so the contract layer has a dedicated file for scenario DTOs.
 */

export type {ScenarioRecord} from "./assets";